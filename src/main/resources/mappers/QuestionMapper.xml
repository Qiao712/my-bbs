<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="github.qiao712.bbs.mapper.QuestionMapper">
    <select id="selectLikeCount" resultType="long">
        SELECT like_count FROM t_question WHERE id = #{questionId}
    </select>

    <select id="selectLikeCountBatch" resultType="long">
        SELECT like_count FROM t_question
        WHERE id IN
        <foreach collection="questionIds" item="questionId" open="(" close=")" separator=",">
            #{questionId}
        </foreach>
    </select>

    <select id="selectViewCount" resultType="long">
        SELECT view_count FROM t_question WHERE id = #{questionId};
    </select>

    <select id="selectViewCountBatch" resultType="long">
        SELECT view_count FROM t_question
        WHERE id IN
        <foreach collection="questionIds" item="questionId" open="(" close=")" separator=",">
            #{questionId}
        </foreach>
    </select>

    <update id="updateLikeCount">
        UPDATE t_question SET like_count = #{likeCount} WHERE id = #{questionId}
    </update>

    <update id="increaseViewCount">
        UPDATE t_question SET view_count = view_count + #{delta} WHERE id = #{questionId}
    </update>

    <update id="updateScore">
        UPDATE t_question SET score = #{score} WHERE id=#{questionId}
    </update>

    <update id="increaseAnswerCount">
        UPDATE t_question SET answer_count = answer_count + #{delta} WHERE id = #{questionId}
    </update>

    <select id="existsById" resultType="Boolean">
        SELECT EXISTS(SELECT * FROM t_question WHERE id = #{questionId}) AS e
    </select>
</mapper>
