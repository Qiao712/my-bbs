<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sdu.addd.qasys.mapper.QuestionMapper">
    <select id="selectLikeCountBatch" resultType="long">
        SELECT like_count FROM t_question
        WHERE id IN
        <foreach collection="questionIds" item="questionId" open="(" close=")" separator=",">
            #{questionId}
        </foreach>
    </select>

    <select id="selectViewCountBatch" resultType="long">
        SELECT view_count FROM t_question
        WHERE id IN
        <foreach collection="questionIds" item="questionId" open="(" close=")" separator=",">
            #{questionId}
        </foreach>
    </select>

    <update id="increaseLikeCount">
        UPDATE t_question SET like_count = like_count + #{delta} WHERE id = #{questionId}
    </update>

    <update id="increaseViewCount">
        UPDATE t_question SET view_count = view_count + #{delta} WHERE id = #{questionId}
    </update>

    <update id="updateScore">
        UPDATE t_question SET score = #{score} WHERE id=#{questionId}
    </update>

    <update id="increaseAnswerCount">
        UPDATE t_question SET qa.t_question.answer_count = t_question.answer_count + #{delta} WHERE id = #{questionId}
    </update>

    <select id="existsById" resultType="Boolean">
        SELECT EXISTS(SELECT * FROM t_question WHERE id = #{questionId}) AS e
    </select>
</mapper>
